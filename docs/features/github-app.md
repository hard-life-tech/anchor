# F-010 — GitHub App auth (later)

Replace the single fine-grained PAT with a GitHub App for multi-repo installs and finer scoping. Documented future upgrade — not a v1 blocker.

**Depends on:** stable Core sync paths.
