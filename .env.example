# Anchor process environment only.
# NEVER export GITHUB_TOKEN into agent tmux panes.
# Copy to `.env` (gitignored) and fill real values. Do not commit `.env`.

# Required
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
GITHUB_USER=your-github-username
ANCHOR_PASSWORD=change-me-to-a-long-random-secret

# Optional auth
# ANCHOR_USER=admin
# ANCHOR_SESSION_SECRET=long-random-string-for-cookie-hmac
# ANCHOR_COOKIE_SECURE=false

# Optional — SQLite settings DB (default: $HOME/projects/anchor.db)
# DATABASE_URL=sqlite:/home/agent/projects/anchor.db
# ANCHOR_DB=/home/agent/projects/anchor.db

# Optional — GitHub.com (defaults) or GitHub Enterprise Server
# GITHUB_HOST=github.com
# GITHUB_API_URL=https://api.github.com
# Enterprise example:
# GITHUB_HOST=github.example.com
# GITHUB_API_URL=https://github.example.com/api/v3

# Optional (Compose also sets PROJECTS_DIR / TMUX_SESSION / PORT)
PROJECTS_DIR=/home/agent/projects
TMUX_SESSION=agents
CURSOR_CMD=cursor-agent
OPENCODE_CMD=opencode
PORT=8080
LOG_LEVEL=info
